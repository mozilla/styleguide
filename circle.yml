dependencies:
  override:
    - npm install

deployment:
  master:
    branch: master
    owner: mozilla
    commands:
        - npm run deploy

general:
    branches:
        only:
            - master
